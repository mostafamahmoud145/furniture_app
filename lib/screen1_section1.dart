import 'package:flutter/material.dart';

class Screen1Section1 extends StatelessWidget {
  const Screen1Section1({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Screen 1 Section 1')),
    );
  }
}
